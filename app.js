import { WindowsModel, TabsModel } from "./model/Models.js";

const express = require('express')
const app = express()
const mongoose = require('mongoose')


app
  .get('/windows', (req, res, next) => {

  })